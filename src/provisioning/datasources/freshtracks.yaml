apiVersion: 1

datasources:
- name: FreshTracks
  type: prometheus
  access: proxy
  orgId: 1
  # using Robust Perception's Prometheus demo server
  url: http://demo.robustperception.io:9090
  password:
  user:
  database:
  basicAuth:
  basicAuthUser:
  basicAuthPassword:
  withCredentials:
  isDefault: true
  jsonData:
  version: 1
  editable: true